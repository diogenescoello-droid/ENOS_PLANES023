document.addEventListener("DOMContentLoaded",async()=>{bindTabs();bindRefresh();try{EnosApi.setStatus("Consultando API","warn");await ModuloRiesgo.init();await ModuloDocumental.init();EnosApi.setStatus("API conectada","ok")}catch(error){console.error(error);EnosApi.setStatus("Error API","error");showBootError(error)}});function bindTabs(){document.querySelectorAll(".tab").forEach(tab=>{tab.addEventListener("click",()=>{document.querySelectorAll(".tab").forEach(t=>t.classList.remove("active"));tab.classList.add("active");const moduleName=tab.dataset.module;document.querySelectorAll(".module").forEach(module=>module.classList.remove("module-active"));const target=document.getElementById(`module-${moduleName}`);if(target)target.classList.add("module-active");setTimeout(()=>{window.dispatchEvent(new Event("resize"))},100)})})}function bindRefresh(){const button=document.getElementById("btn-refresh");if(!button)return;button.addEventListener("click",()=>{window.location.reload()})}function showBootError(error){const target=document.querySelector(".module-active .module-header")||document.body;const message=document.createElement("div");message.className="table-card";message.innerHTML=`<h3>No se pudo cargar la información</h3><p>${escapeHtml(error.message||error)}</p><p class="muted">Revise API_BASE_URL y API_TOKEN en <code>assets/js/config.js</code>. En producción, si aparece bloqueo CORS, consuma la API desde backend de la plataforma ENOS.</p>`;target.after(message)}function escapeHtml(value){return String(value??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;")}